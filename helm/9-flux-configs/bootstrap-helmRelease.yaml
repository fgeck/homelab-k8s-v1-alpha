apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: flux-tests
  namespace: flux-system
spec:
  interval: 10m
  chart:
    spec:
      chart: helm/8-flux-tests/
      version: '0.0.1'
      sourceRef:
        kind: GitRepository
        name: homelab-k8s
        namespace: flux-system
      interval: 10m